import { useEffect, useState, useRef } from 'react'
import './App.css'
import { useInput, useTitle } from './custom-hooks';



function App() {
  // const validate = (value: string) => value.length < 5;
  // const { value: inputValue, onChange: onChangeForInput } = useInput(0, validate)
  // const titleUpdater = useTitle('Loading...');
  // const input = useRef();
  return (
    <>
      <div>
      </div>


    </>
  )
}

export default App
